import CSS from 'csstype';
import { TOfNull, TOption } from './types';
interface IProps<T> {
    className?: string;
    style?: CSS.Properties;
    onChange?: (value: TOfNull<T>) => void;
    onClick?: (value: TOfNull<T>) => void;
    isSearchEnable?: boolean;
    isCheckedEnable?: boolean;
    isAvatarEnable?: boolean;
    value?: TOfNull<T>;
    options?: TOption<TOfNull<T>>[];
    searchTextPlaceholder?: string;
    isDark?: boolean;
}
/**
 * 時間選擇器
 * @param className
 * @param style
 * @param options
 * @param onChange 選擇視窗當項目異動時
 * @param value Input Value
 * @param searchTextPlaceholder
 * @param isVisibleSearchText
 * @param isDark 暗黑模式
 */
declare const Dropdown: <T extends unknown>({ className, style, options, value, onChange, onClick, searchTextPlaceholder, isSearchEnable, isCheckedEnable, isAvatarEnable, isDark, }: IProps<T>) => JSX.Element;
export default Dropdown;
