import { IDropdownGroupOption, IDropdownOption, TOption } from './types';
/**
 * 檢查傳入類型
 * @param options
 */
export declare const isGroupOptions: <T>(options: TOption<T>) => options is IDropdownGroupOption<T>;
/**
 * 過濾項目
 * @param options
 * @param filterKeyword
 */
export declare const filterOptions: <T>(options: IDropdownOption<T>[], filterKeyword: string) => IDropdownOption<T>[];
/**
 * 判定是否為空
 * @param value
 * @returns {boolean}
 */
export declare function isEmpty(value: any): boolean;
/**
 * 刪除陣列中的一筆資料 (immutable)
 * ps: 不用先複製, 方法內會複製出來
 * ex: [1,2,3] -> [1,3]
 *
 * @param arrayData
 * @param index
 */
export declare function removeByIndex<T>(arrayData: T[], index: number): T[];
